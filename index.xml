<?xml version="1.0" encoding="UTF-8"?>
<!-- 关键：ReaPack 仅认 <reapack> 为根元素，绝对不能用 <index>！ -->
<reapack>
    <!-- 1. 强制：仓库元数据（ReaPack 识别仓库的唯一依据） -->
    <repositories>
        <repository 
            name="ReaperTools"                <!-- 仓库名称（任意，非空） -->
            author="XFeng"                     <!-- 你的名字（非空） -->
            url="https://github.com/XFeng11/ReaperTools/raw/refs/heads/main/index.xml"  <!-- 必须与你的导入链接完全一致 -->
            description="REAPER 实用工具集：轨道管理、标记操作"  <!-- 非空描述 -->
        />
    </repositories>

    <!-- 2. 强制：至少一个分类 + 一个脚本包（否则仓库为空） -->
    <category name="Tools">  <!-- 分类名称（非空，如“工具”“轨道”） -->
        <package name="ReaperTools: 核心工具" type="script">  <!-- 包名称（非空），类型固定为 script -->
            <!-- 3. 强制：版本信息（必须有版本号、作者、时间） -->
            <version 
                name="1.0.0"                  <!-- 版本号（如 1.0.0） -->
                author="XFeng"                <!-- 与仓库作者一致 -->
                time="2025-09-10T12:00:00Z"   <!-- 时间格式（UTC，可改时分） -->
                compatibility="REAPER 6.0+"    <!-- 兼容版本（非强制但建议加） -->
            >
                <!-- 4. 强制：至少一个脚本文件（URL 必须能访问到实际 .lua 文件） -->
                <file 
                    target="Scripts/F_region范围适应items.lua"  <!-- 安装路径（REAPER 脚本目录下的路径） -->
                    url="https://github.com/XFeng111/ReaperTools/raw/refs/heads/main/Toolbox_Custom/F_Tool/F_region范围适应items.lua"  <!-- 你脚本的 Raw 链接（需确保存在） -->
                    type="script"              <!-- 文件类型（脚本固定为 script） -->
                    main="true"                <!-- 标记为主脚本（非强制，方便用户找到核心功能） -->
                />
            </version>

            <!-- 5. 非强制但建议：包描述（帮助用户了解功能） -->
            <metadata>
                <description>ReaperTools 包含轨道管理、标记适配等实用脚本，提升 REAPER 制作效率。</description>
                <tags>轨道管理, 标记操作, REAPER 工具</tags>  <!-- 搜索标签（方便用户找到） -->
                <homepage>https://github.com/XFeng11/ReaperTools</homepage>  <!-- 你的仓库主页 -->
            </metadata>
        </package>
    </category>

    <!-- 6. 非强制：全局仓库描述 -->
    <metadata>
        <description>ReaperTools 仓库：专注于 REAPER 脚本开发，提供高效制作工具。</description>
    </metadata>
</reapack>
